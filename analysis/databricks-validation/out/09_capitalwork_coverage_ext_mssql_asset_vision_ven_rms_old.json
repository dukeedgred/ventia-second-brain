{
  "name": "09_capitalwork_coverage_ext_mssql_asset_vision_ven_rms_old",
  "statement": "SELECT\n  'ext_mssql_asset_vision_ven_rms_old' AS catalog_name,\n  COUNT(*) AS rows_total,\n  SUM(CASE WHEN Contract IS NOT NULL AND Contract <> '' THEN 1 ELSE 0 END) AS rows_with_contract,\n  SUM(CASE WHEN AssetID IS NOT NULL THEN 1 ELSE 0 END) AS rows_with_asset_id,\n  SUM(CASE WHEN PlannedStart IS NOT NULL THEN 1 ELSE 0 END) AS rows_with_planned_start,\n  SUM(CASE WHEN ActualFinish IS NOT NULL THEN 1 ELSE 0 END) AS rows_with_actual_finish,\n  SUM(CASE WHEN ChainageFrom IS NOT NULL OR ChainageTo IS NOT NULL THEN 1 ELSE 0 END) AS rows_with_chainage,\n  SUM(CASE WHEN SpatialInfo IS NOT NULL THEN 1 ELSE 0 END) AS rows_with_spatial_info\nFROM `ext_mssql_asset_vision_ven_rms_old`.dbo.capitalwork\nWHERE Deleted = false OR Deleted IS NULL",
  "status": {
    "state": "FAILED",
    "error": {
      "error_code": "BAD_REQUEST",
      "message": "[TABLE_OR_VIEW_NOT_FOUND] The table or view `ext_mssql_asset_vision_ven_rms_old`.`dbo`.`capitalwork` cannot be found. Verify the spelling and correctness of the schema and catalog.\nIf you did not qualify the name with a schema, verify the current_schema() output, or qualify the name with the correct schema and catalog.\nTo tolerate the error on drop use DROP VIEW IF EXISTS or DROP TABLE IF EXISTS. SQLSTATE: 42P01; line 10 pos 5"
    },
    "sql_state": "42P01"
  },
  "manifest": null,
  "result": null
}
